# Copy to .env.local for local dev. Read by lib/api.ts as the backend base URL.
# Deploy lane sets the equivalent Vercel env var to the deployed Render URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
